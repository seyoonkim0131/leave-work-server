type Schedule {
    id: Int!
    userId: Int!
    user: User!
    date: String!
    startTime: String
    endTime: String
    overTime: String
    break: String!
}

type Query {
    schedule: Schedule
}